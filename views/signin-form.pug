extends layout.pug

block content
	section.container
		h1.header= title
	article.item
		form.form(method="post", action="/signin")
			fieldset
				p
					label Nombre :
					input#username(type="text", name="username", required).i-form
				p
					label Contraseña :
					input#password(type="password", name="password", required).i-form
				p
					label Fecha del Evento :
					input#date(type="date", name="date", required).i-form
				p
					label Lugar del Evento :
					input#place(type="text", name="lugar", required).i-form
				div.btns
					div.btn-cont
						input.btn.show(type="submit", value="Registrarse")
					div.btn-cont
						a.btn.show(href="/login") Entrar
